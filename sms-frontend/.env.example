# Backend API base URL (sms-nest). The backend reads PORT from its own .env
# (currently 4008). No global prefix, no trailing /api.
VITE_API_BASE_URL=http://localhost:4008

# Admin contact shown in the "request plan" dialog (no window is opened).
VITE_SUPPORT_PHONE=+998911786007
VITE_SUPPORT_TELEGRAM=Bunyod_IYB
